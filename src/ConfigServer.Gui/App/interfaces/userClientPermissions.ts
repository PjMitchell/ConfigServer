export interface IUserClientPermissions  {
        canAccessClientAdmin: boolean;
        canEditClients: boolean;
        canEditGroups: boolean;
        canDeleteArchives: boolean;
        hasClientConfiguratorClaim: boolean;
}
